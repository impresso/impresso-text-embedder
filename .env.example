# Save this file as .env or make sure that your environment knows them!
SE_ACCESS_KEY=
SE_SECRET_KEY=
SE_HOST_URL=https://os.zhdk.cloud.switch.ch

HARBOR_ROBOT_USERNAME=
HARBOR_ROBOT_PASSWORD=

# EPFL RCP AIaaS — required by impresso-research-query-generate (step 4 of
# the chunking-eval branch). Get a key from https://portal.rcp.epfl.ch/aiaas/keys
# (EPFL VPN required). OpenAI-compatible; passed as Bearer token to
# https://inference.rcp.epfl.ch/v1.
RCP_API_KEY=